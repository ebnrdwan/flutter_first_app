import 'package:flutter/material.dart';


class newsc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(
      body: Text("new screen is here", textAlign: TextAlign.center,),

    ) //home
    );
  }
}

